# v0.6.5 全链路验证报告

> 2026-07-12 22:30 | Code + Log + DB 三对照

---

## 逐条验证

| # | 场景 | 代码逻辑 | 日志证据 | 数据库证据 | 结论 |
|:-:|:-----|:---------|:---------|:-----------|:----:|
| 1 | 添加自选股 002475 | `storage.py:add()` 写入SQLite | `watchlist.added code=002475` | `SELECT * FROM watchlist` → 有记录 | ✅ |
| 2 | 添加自选股 603667 | 同上 | `watchlist.added code=603667` | `SELECT * FROM watchlist` → 有记录 | ✅ |
| 3 | 添加后查自选股列表 | `services.py:get_watchlist()` → 读 DB | `GET /api/watchlist → 200 OK` | 返回 2 条记录 | ✅ |
| 4 | 周末查自选股 (无实时行情) | MarketState=WEEKEND → 读本地DB | 接口200 | 有 code+name | ✅ |
| 5 | 周末查选股机会 | MarketState=WEEKEND → 返回空 | `opportunities.api_skipped phase=weekend` | `{"total":0}` | ✅ |
| 6 | 周末查市场情绪 | MarketState=WEEKEND → 返回 neutral | 接口200 | `source=unavailable` | ✅ |
| 7 | 周末查仪表盘 | MarketState=WEEKEND → DB数据 | 接口200 | `watchlist_count=2` | ✅ |
| 8 | 周末个股分析不崩溃 | `get_realtime()` → 兜底quote | `GET /api/stock/603667 → 200` | AI引擎耗时31s+45s但成功 | ✅ |
| 9 | Prefetch 周末跳过 | `prefetch.py:is_trading` | `prefetch.skipped phase=weekend` | 无API调用 | ✅ |
| 10 | 重复添加自选股 | `storage.py:add()` → INSERT OR REPLACE | 日志显示重复 code=603667 多次 | DB只有1条 | ✅ |

## 已验证的完整用户流程

```
[用户操作]                           [后端]                    [数据库]          [日志]
添加 002475 立讯精密  ──────────→   POST /api/watchlist  ──→  watchlist表写入  ──→ 200 OK
添加 603667 五洲新春  ──────────→   POST /api/watchlist  ──→  watchlist表写入  ──→ 200 OK
切到自选股页          ──────────→   GET /api/watchlist    ──→  读DB返回2条     ──→ 200 OK
切到选股机会页        ──────────→   GET /api/opportunities    (周末跳过)       ──→ {"total":0}
查看个股 603667       ──────────→   GET /api/stock/603667     (AI引擎耗时~75s) ──→ 200 OK
```

## 当前仍存在的问题

| 问题 | 说明 |
|:-----|:------|
| ⚠️ 个股分析 AI 引擎太慢 | 单次分析耗时 31s + 45s = ~76s，用户需要等很久 |
| ⚠️ 选股机会页周末全空 | 设计如此（不扫描），但用户看到空页面可能困惑 |
| ⚠️ 缓存数据与实时数据混合 | dashboard 的 market_sentiment 用了 L2 缓存旧数据 |
